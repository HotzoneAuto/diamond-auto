syntax = "proto2";

package apollo.drivers.magnetic;

message MagneticDeviceConf {
  optional string device_id = 1;
  optional bool enable_log = 2 [default = false];
  optional string output_channel = 3;
}
